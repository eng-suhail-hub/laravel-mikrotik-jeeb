<?php

/**
 * ════════════════════════════════════════════════════════════════
 *  إعدادات محفظة جيب (Webhook Receiver)
 * ════════════════════════════════════════════════════════════════
 *
 *  محفظة جيب ترسل إشعارات الدفع عبر Android Emulator، والإشعارات
 *  تصل إلى Laravel عبر Webhook. القيم هنا تتحكم في كيفية استقبال
 *  وتحليل الإشعارات.
 */

return [

    // المفتاح السري (API Key) لحماية الـ Webhook
    // يجب أن يطابق المفتاح المُعد في الـ Emulator
    'webhook_secret' => env('JEEB_WEBHOOK_SECRET', 'change-this-strong-secret-key'),

    // اسم حزمة محفظة جيب (للتعرف عليها من Emulator عند بث الإشعارات)
    'emulator_package' => env('JEEB_EMULATOR_PACKAGE', 'com.jeeb.wallet'),

    // رسالة التنبيه للمستخدم عند إنشاء الحساب
    // تُرسل في استجابة API حتى يعرضها تطبيق Flutter كـ Dialog
    'registration_warning' => env(
        'JEEB_REGISTRATION_WARNING',
        'يجب أن يكون الاسم الرباعي ورقم الهاتف مطابقين تماماً لبيانات حسابك في محفظة جيب، وإلا لن يتم توليد الكرت.'
    ),

    // أنماط الـ Regex لاستخراج البيانات من نص الإشعار الخام
    // ⚠️ كلاس WebhookParser يحتوي على المنطق الفعلي
    'parser' => [
        // نمط استخراج المبلغ (مثلاً: 5000 ريال، 5,000 ر.ي)
        'amount_pattern' => '/(\d{1,3}(?:[,\s]\d{3})*|\d+)\s*(?:ر\.ي|ريال|ر يال|YER|USD|\$)?/u',

        // نمط استخراج رقم الهاتف (يمني: 7xxx xxx xxx أو 967xxxxxxxxx)
        'phone_pattern' => '/(?:\+?967|0)?7\d{8}/u',

        // نمط استخراج رقم المرجع (Reference / Transaction ID)
        'reference_pattern' => '/(?:Ref|ref|مرجع|REF)[:#\s]*([A-Z0-9\-]{4,})/u',

        // نمط استخراج الاسم (بعد كلمة "إلى" أو "لـ")
        'name_pattern' => '/(?:إلى|لـ|الى|from|to)\s+([\p{L}\s]{4,})/u',
    ],
];
